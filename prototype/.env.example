# Backend API (ajusta cache runtime do PWA)
# Quando usar backend externo:
# VITE_API_ORIGIN=https://api.suaempresa.com
# VITE_API_PREFIX=/v1/

# Quando usar backend no mesmo host do app (default):
VITE_API_PREFIX=/api/

# WebSocket mock de dispositivos (balança/impressão)
VITE_WS_HOST=localhost
VITE_WS_PORT=8787

# Timings de lock de comanda (ms)
VITE_LOCK_TTL_MS=15000
VITE_LOCK_HEARTBEAT_MS=10000
