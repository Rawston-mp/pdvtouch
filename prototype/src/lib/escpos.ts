// src/lib/escpos.ts
// Mock básico para ESC/POS

export function buildCustomerCoupon(params: any): Uint8Array {
  // Mock: retorna dados vazios
  return new Uint8Array([])
}

export function ticketKitchen(params: any): Uint8Array {
  // Mock: retorna dados vazios
  return new Uint8Array([])
}

export function tefReceipt(params: any): Uint8Array {
  // Mock: retorna dados vazios
  return new Uint8Array([])
}